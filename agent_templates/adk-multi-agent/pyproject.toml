[project]
name = "agent-name"
version = "0.1.0"
dependencies = [
    "google-adk>=1.0.0",
    "google-cloud-aiplatform[reasoningengine]>=1.50.0",
    "cloudpickle>=3.0.0",
]

[tool.setuptools]
packages = ["tools"]
